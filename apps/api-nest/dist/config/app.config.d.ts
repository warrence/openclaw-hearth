export declare const appConfig: (() => {
    name: string;
    environment: string;
    host: string;
    port: number;
    prefix: string;
    logLevel: string;
}) & import("@nestjs/config").ConfigFactoryKeyHost<{
    name: string;
    environment: string;
    host: string;
    port: number;
    prefix: string;
    logLevel: string;
}>;
